# #78186: design-system-mcp: Add server instructions for client usage guidance

- **Source:** WordPress/gutenberg
- **Type:** Pull request
- **Author:** @aduth
- **Labels:** `[Type] Enhancement`, `[Package] Components`, `[Package] Design System MCP`
- **Merged:** [`2639f0d`](https://github.com/WordPress/gutenberg/commit/2639f0dc01680968e13bc95876fdcb6fcce93624)
- **Discussion:** [#78186](https://github.com/WordPress/gutenberg/pull/78186) · 4 comments · 0 reactions

## Summary

The WordPress Design System MCP server now exposes structured server instructions to guide AI clients on proper usage workflows. This enhancement improves agent discoverability and operational efficiency by explicitly defining scope, recommended tool sequencing, and preference hierarchies before tool discovery occurs.

## Impact

- **AI/MCP Clients & Integrations:** Receive guidance via the standard MCP `instructions` parameter, enabling better context routing for design-system requests without requiring client-side code changes.
- **Component/Theme Developers:** No breaking changes or code modifications required. The instructions clarify that workflows should rely on canonical component APIs rather than manual CSS custom property manipulation.
- **Platform/Hosting Teams:** No action required; this is an internal MCP server configuration update with no effect on standard WordPress runtime behavior.

## Technical details

- Updates the `wordpress-design-system` MCP server definition to inject a structured `instructions` block.
- The text explicitly outlines a recommended client workflow: invoke `get_components` first to map generic/fuzzy user names to canonical exports, then call `get_component_details` for specific implementation data.
- Defines scope boundaries (`@wordpress/components` and `@wordpress/ui` packages) and establishes a preference hierarchy favoring component APIs over raw semantic tokens exposed as `--wpds-*` CSS custom properties.
- Acknowledges that detailed failure modes and output shape hints are currently out of scope; these items were flagged in discussion as candidates for future formalization via JSON Schema updates to the tool definitions.

## Contribution

Opened and merged by `@aduth` (commit `2639f0d`), with co-authors `@mirka` and `@ciampo`. The initial instruction draft was generated by Claude Code Opus 4.7 and heavily edited by the author. The PR was temporarily branched against #78185 to resolve a text dependency, with final review noting opportunities to formalize failure modes and JSON Schema output hints in subsequent iterations.

---

*This content is AI-generated and may contain errors. See [WP Digests](https://github.com/philltran/wp-digests/) — verify against the linked upstream source. Not affiliated with or endorsed by the WordPress project, the WordPress Foundation, or Automattic.*
