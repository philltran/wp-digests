# #79886: Build: Fix non-breaking npm audit vulnerability alerts

- **Source:** WordPress/gutenberg
- **Type:** Pull request
- **Author:** @im3dabasia
- **Labels:** `npm Packages`, `[Type] Security`
- **Merged:** [`9bfe744`](https://github.com/WordPress/gutenberg/commit/9bfe744dbf6207d2a865d56702e106d485839786)
- **Discussion:** [#79886](https://github.com/WordPress/gutenberg/pull/79886) · 1 comments · 0 reactions
- **Usefulness:** 2/5

## Summary

This pull request updates the Gutenberg build dependency tree by running `npm audit fix`, resolving 21 non-breaking security vulnerabilities across transitive and direct dependencies. The change reduces the total vulnerability count from 87 to 66 by patching known issues like command injection, path traversal, and prototype pollution in packages such as `lodash`, `shell-quote`, `basic-ftp`, and `@babel/plugin-transform-modules-systemjs`. It matters because it hardens the development environment and build tooling without introducing breaking changes or requiring manual version pinning.

## Impact

- **Plugin & theme developers / build tooling users:** No immediate code changes required. The update applies to the `package-lock.json` used during local development and CI builds.
- **Hosting & platform teams:** No action required for end-user sites, as this affects only the Gutenberg build toolchain, not the runtime WordPress codebase.
- **Security/Compliance:** Reduces the attack surface of the development/build environment by patching critical and high-severity vulnerabilities in transitive dependencies.
- **Action required:** None. Developers should run `npm install` or `npm ci` to pull the updated lockfile.

## Technical details

The diff modifies only `package-lock.json`, applying version bumps and dependency tree rewrites generated by `npm audit fix`. Key changes visible in the diff include:
- Bumping `@babel/plugin-transform-modules-systemjs` from `7.25.9` to `7.29.7`, along with its Babel helper dependencies (`@babel/helper-module-transforms`, `@babel/helper-plugin-utils`, `@babel/helper-validator-identifier`, `@babel/traverse`).
- Updating all `@nx/nx-*` platform binaries from `22.7.5` to `22.7.6`.
- Removing nested vulnerable dependency blocks under `@php-wasm/fs-journal`, `@php-wasm/node`, and `@php-wasm/web` (specifically `body-parser`, `express`, `iconv-lite`, `safe-buffer`, and `statuses`), which resolves transitive audit alerts.
- The lockfile now reflects a flattened, patched dependency graph without manual `package.json` version overrides. No public APIs, hooks, or runtime behaviors are altered.

## Contribution

Opened and merged by @im3dabasia with co-authorship from @manzoorwanijk. The PR was generated by running `npm audit fix` to address Dependabot alerts, with the author manually reviewing the output and diff before committing. The approach focused strictly on non-breaking patches, avoiding manual version pinning or major dependency upgrades. No significant design debate occurred; the change was accepted as a routine security maintenance update.

---

*This content is AI-generated and may contain errors. See [WP Digests](https://github.com/philltran/wp-digests/) — verify against the linked upstream source. Not affiliated with or endorsed by the WordPress project, the WordPress Foundation, or Automattic.*
